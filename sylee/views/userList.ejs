<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <title>Sunyong list</title>
</head>
    <body>
        <h2>DB 리스트 확인</h2>
        <table border='1'>
            <colgroup>
                <col width='60'><col width='200'><col width='230'>
            </colgroup>
            <thead>
                <tr>
                    <th>번호</th>
                    <th>이름</th>
                    <th>생성일자</th>
                    <th>삭제</th>
                </tr>
            </thead>
            <tbody>
                <% for(i = 0; i < list.length; i++) { %>
                <tr>
                    <td><%=list[i].num %></td>
                    <td><%=list[i].name %></td>
                    <td><%=list[i].create_time %></td>
					<form action='/userList' method='post'>
	                    <td><button type='submit'>삭제</button>
							<input type='hidden' name='del' value=<%=list[i].name %>></td>
					</form>
                </tr>
                <% } %>	
            </tbody>
        </table boader='1'>
	<form action='/insert' method='post'>
		<br>Client_id : <input type='text' name='add_id'>
		<button type='submit'>DB 추가</button>
	</form>
    </body>
</html>
